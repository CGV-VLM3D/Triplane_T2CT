# Tier-2 reference baseline: TriVQAEConv at 1mm full latent.

defaults:
  - data: latents_1mm
  - model: trivqae_conv
  - train: tier2
  - eval: full
  - loss: l1
  - wb: default
  - _self_

exp_name: trial_mid_baseline

hydra:
  run:
    dir: runs/${exp_name}/hydra/${now:%Y-%m-%d_%H-%M-%S}
