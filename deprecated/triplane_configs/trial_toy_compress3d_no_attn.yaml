# Tier-1 toy: Compress3D TriplaneAE without CrossAttention3D (ablation), 2mm.

defaults:
  - data: latents_2mm
  - model: triplane_ae
  - train: tier1
  - eval: latent_only
  - loss: l1
  - wb: default
  - _self_

exp_name: trial_toy_compress3d_no_attn

model:
  encoder:
    latent_shape: [60, 60, 32]
    use_cross_attn: false

loss:
  kl_weight: 1.0e-6

hydra:
  run:
    dir: runs/${exp_name}/hydra/${now:%Y-%m-%d_%H-%M-%S}
