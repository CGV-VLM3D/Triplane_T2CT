model:
  encoder:
    in_channels: 4
    emb_dim: 512
    n_layers: 4
    n_heads: 8
    out_channels: 8
    latent_shape: [120, 120, 64]
  decoder:
    in_channels: 8
    hidden: 32
    out_channels: 4
    latent_shape: [120, 120, 64]

loss:
  l1_weight: 1.0
  lpips_weight: 0.0
  gan_weight: 0.0
