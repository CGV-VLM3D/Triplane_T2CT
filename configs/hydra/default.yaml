# https://hydra.cc/docs/configure_hydra/intro/

# enable color logging
defaults:
  - override hydra_logging: colorlog
  - override job_logging: colorlog

# output directory, generated dynamically on each run.
# dated_rundir (src/utils/resolvers.py): KST date, with _2/_3… on same-day collision
# → outputs/<task_name>/2026-06-22 , 2026-06-22_2 , … (replaces UTC-clock HH-MM-SS).
run:
  dir: ${dated_rundir:${paths.output_root}/${task_name}}
sweep:
  dir: ${dated_rundir:${paths.output_root}/${task_name}/multiruns}
  subdir: ${hydra.job.num}

job_logging:
  handlers:
    file:
      # Incorporates fix from https://github.com/facebookresearch/hydra/pull/2242
      filename: ${hydra.runtime.output_dir}/${task_name}.log
