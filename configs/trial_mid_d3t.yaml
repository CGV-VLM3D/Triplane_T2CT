# Tier-2: TriVAE_D3T at the full 1mm latent.

defaults:
  - data: latents_1mm
  - model: trivae_d3t
  - train: tier2
  - eval: full
  - loss: l1
  - wb: default
  - _self_

exp_name: trial_mid_d3t

train:
  amp: bf16

hydra:
  run:
    dir: runs/${exp_name}/hydra/${now:%Y-%m-%d_%H-%M-%S}
