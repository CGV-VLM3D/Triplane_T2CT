# @package _global_.model
name: generatect

_target_: src.eval.samplers.generatect.GenerateCTSampler

# GenerateCT uses pretrained checkpoints; no ckpt_path needed (uses third_party).
n_samples_per_case: 1
