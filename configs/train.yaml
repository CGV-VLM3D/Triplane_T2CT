# @package _global_

# wandb override cheat-sheet (CLI 뒤에 그냥 붙이면 됨):
#   logger.wandb.project=my_project          # wandb Project 탭 이름 (기본 vlm3d)
#   logger.wandb.name=my_run_v1              # run 이름을 직접 지정 (기본 task_name_timestamp)
#   logger.wandb.group=ablation-lr           # run 묶음 그룹
#   예: python src/train.py experiment=report2ct_fvlm logger.wandb.name=fvlm_lr1e4 logger.wandb.project=report2ct

# specify here default configuration
# order of defaults determines the order in which configs override each other
defaults:
  - _self_
  # data/model are picked via `experiment=...` or CLI override (e.g. `data=ct_rate model=report2ct`).
  - data: null
  - model: null
  - callbacks: default
  - logger: wandb
  - trainer: default
  - paths: default
  - extras: default
  - hydra: default

  # experiment configs allow for version control of specific hyperparameters
  # e.g. best hyperparameters for given model and datamodule
  - experiment: null

  # config for hyperparameter optimization
  - hparams_search: null

  # optional local config for machine/user specific settings
  # it's optional since it doesn't need to exist and is excluded from version control
  - optional local: default

  # debugging config (enable through command line, e.g. `python train.py debug=default)
  - debug: null

# task name, determines output directory path (outputs/<task_name>/<KST-date>[_N]/, _2/_3… on same-day reruns)
task_name: "train"

# tags to help you identify your experiments
# you can overwrite this in experiment configs
# overwrite from command line with `python train.py tags="[first_tag, second_tag]"`
tags: ["dev"]

# set False to skip model training
train: True

# evaluate on test set, using best model weights achieved during training
# lightning chooses best weights based on the metric specified in checkpoint callback
test: True

# simply provide checkpoint path to resume training
ckpt_path: null

# seed for random number generators in pytorch, numpy and python.random
seed: null
